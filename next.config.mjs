/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
    images: {
    domains: ["img.youtube.com"], // Add YouTube thumbnail domain here
  },
  reactCompiler: true,
};

export default nextConfig;
